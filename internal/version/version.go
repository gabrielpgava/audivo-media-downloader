package version

// Current is replaced by the release build when a distribution needs a
// different version. Keeping a source default makes diagnostics useful in
// local builds too.
const Current = "0.1.0"
